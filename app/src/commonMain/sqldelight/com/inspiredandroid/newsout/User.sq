CREATE TABLE user (
  sorting INTEGER NOT NULL,
  isFolderTop INTEGER NOT NULL
);

INSERT INTO user (sorting, isFolderTop)
VALUES (1, 0);

selectAll:
SELECT *
FROM user;

updateSorting:
UPDATE user SET sorting = ?;

updateFolderTop:
UPDATE user SET isFolderTop = ?;
